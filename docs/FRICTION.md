# 마찰 감사 (Friction Audit)

> 느낌이 아니라 **센 숫자**로 관리한다.
> `BASE=http://localhost:3001 node qa/audit.mjs` 가 실제로 화면을 조작하면서
> 사람이 몇 번 손대야 하는지를 세고, 목표를 넘으면 실패로 처리한다.
>
> 측정 환경: 390×844 (실제 주 사용 기기), 로컬 저장 모드.

## 왜 자동으로 세는가

"편해졌나?" 는 물어볼 때마다 답이 달라진다. 클릭 수는 안 그렇다.
기능을 더하다 보면 조용히 한 단계가 늘어나는데, 사람은 그걸 잘 못 느낀다.
그래서 열여섯 개 시나리오의 손동작 수를 매번 세고, 늘어나면 CI 에서 걸리게 했다.

## 현재 (2026-09-20)

| 시나리오 | 탭 | 입력 | 합계 | 목표 | 비고 |
|---|---:|---:|---:|---:|---|
| A. 오늘 할 일 확인 | 0 | 0 | **0** | 0 | 홈 첫 화면 |
| B. 할 일 추가 (홈 FAB) | 3 | 1 | **4** | 4 | |
| B2. 할 일 추가 (목록) | 1 | 1 | **2** | 2 | 제목만 치고 엔터 |
| C. 할 일 완료 | 1 | 0 | **1** | 1 | 목록에서 원탭 |
| D. 일정 추가 (시간 포함) | 3 | 2 | **5** | 5 | |
| E. 비용 추가 | 3 | 1 | **4** | 5 | |
| F. 금액 수정 | 2 | 1 | **3** | 3 | 목록에서 금액 탭 → 숫자패드 → 확인 |
| G. 결제 완료 | 1 | 0 | **1** | 1 | 홈에서 원탭 |
| H. 하객 추가 | 1 | 1 | **2** | 2 | 이름만 치고 엔터 |
| I. 참석 상태 변경 | 1 | 0 | **1** | 1 | 목록에서 원탭 |
| J. 청첩장 전달 | 1 | 0 | **1** | 1 | 목록에서 원탭 |
| K. 업체 확인 → 수정 | 2 | 0 | **2** | 3 | |
| L. 신혼여행 확인 | 0 | 0 | **0** | 1 | 첫 화면에 출발 정보 |
| M. 메모 기록 | 3 | 1 | **4** | 4 | |
| N. 삭제 → 실행 취소 | 3 | 0 | **3** | 3 | |
| O. 누가 바꿨는지 | 0 | 0 | **0** | 1 | 홈에 최근 활동 |

**16개 전부 목표 이내.**

## 이번 감사에서 실제로 찾아 고친 것

### 1. 방금 추가한 비용 항목이 사라진다 (가장 나빴던 것)

금액 없는 예산 항목은 접어 두는 게 맞다 — 이관된 빈 항목이 21개나 있다.
그런데 새 비용을 추가하면 금액이 0이라 **곧바로 그 접힘 속으로 사라졌다.**
사용자에게는 "저장이 안 됐나?" 가 된다.

고친 방법: 이 탭에서 직접 만든 것은 접힘 규칙을 건너뛴다 (`src/lib/fresh.ts`).

> 처음엔 '만든 시각이 최근인가' 로 판단했는데 틀렸다. 원본 이관 데이터도
> 처음 켠 순간 전부 '방금' 이 되어 버려서(`buildMigratedData` 가 지금 시각을 찍는다)
> 접힘이 통째로 풀렸다. 시계를 보지 않고 실제로 `add()` 한 id 만 기억하도록 바꿨다.
> sessionStorage 에 둬서 새로고침해도 남고 탭을 닫으면 사라진다.

### 2. 금액 수정이 시트 안의 시트였다 (5번 → 3번)

목록 → 상세 시트 → 금액 버튼 → **또 시트** → 숫자 → 닫기 → 닫기.
결혼 준비 중 가장 자주 하는 일 치고 너무 멀었다.

고친 방법: 목록 줄의 **예상 / 실제 금액을 각각 직접 누르면** 숫자패드가 바로 뜬다.
상세 시트를 열 필요가 없다.

### 3. 식장 화면이 하객 명단을 안 쓰고 있었다 (데이터 정합성)

식장에 손으로 적어 둔 '예상 인원' 으로 식대를 계산하고 있었다.
앱이 이미 하객 명단에서 진짜 인원을 알고 있는데도.
같은 숫자를 두 군데서 관리하면 반드시 어긋난다.

고친 방법: `computeMealEstimate()` 한 곳에서만 센다. 명단이 있으면 명단이 정답,
비었을 때만 적어 둔 값을 쓴다.

### 4. 계약 식장 이름을 눌러도 아무 일이 없었다

고치려는 사람은 제목을 먼저 누른다. 이름도 상세를 열도록 했다.

## 데이터 정확성 (13가지 전부 통과)

같은 숫자가 화면마다 같은지를 **원장부에서 직접 센 값과 비교**한다.
앱의 계산을 앱의 계산과 비교하면 둘 다 틀려도 통과하므로, 기준은 늘 원본 데이터다.

- 홈 '사용 금액' = 실제금액 합계
- 예산 '총 실제 비용' = 실제금액 합계 / '총 결제 금액' = 결제완료 합계
- **홈과 예산이 같은 '사용 금액'** 을 보여주는가
- 하객 '예상 총 인원' = 불참 뺀 (본인 + 동반)
- 신랑 + 신부 + 공통 = 전체 (겹쳐 세지 않음)
- 참석확정 ≤ 예상 인원
- 홈 '준비 진행률' = 완료 / 전체
- D-Day, 결혼식 날짜, 음수 금액 · 음수 인원 · 1900년대 날짜 없음

## 목표를 정할 때 쓴 기준

- **확인만 하는 일은 0번.** 홈을 열면 이미 보여야 한다.
- **자주 하는 상태 변경은 1번.** 완료 · 참석 · 청첩장 · 결제.
- **추가는 2번** (목록에서 제목만) 또는 **4~5번** (FAB 으로 자세히).
- **수정은 3번 이내.**

## 다음에 볼 것

- 하객이 100명을 넘었을 때 목록 스크롤과 검색 체감
- 날짜 바꾸기(마감일 · 일정)의 손동작 수 — 지금은 측정 항목에 없다

---

# 접근성 점검 (`qa/a11y.mjs`)

10가지 전부 통과. 이 감사에서 실제로 찾아 고친 것:

### 1. 글자를 125% 로 키우면 세 화면이 가로로 터졌다

예산 · 웨딩 준비 · 설정에서 페이지 전체에 가로 스크롤이 생겼다.
원인은 CSS 기본값 `min-width: auto` — 그리드 칸이 '내용의 최소 너비' 아래로 안 줄어든다.
글자가 커지면 안쪽 내용이 칸을 밀어내고, 칸이 화면 밖으로 삐져나간다.

```css
:where(.grid) > * { min-width: 0; }
```

한 줄로 아홉 화면이 전부 고쳐졌다. `:where()` 라 우선순위가 0이어서 필요하면 덮을 수 있다.

### 2. 영역 색을 '글자' 로 쓸 때 대비가 모자랐다 (12쌍 중 11쌍)

영역 색 6종은 **흰 바탕에서 표식(3px 띠 · 아이콘)으로** 쓰려고 고른 값이라
자기 soft 배경 위의 작은 글자로는 4.5:1 에 못 미쳤다 (라이트 3.03~4.23, 다크 3.92~4.55).

색을 바꾸지 않고, 같은 색상 그대로 밝기만 옮긴 **글자 전용 토큰**(`--tint-*-text`)을 따로 뒀다.
아이콘은 그림이라 3:1 이면 되므로 기존 색을 그대로 쓴다.

> 이건 팔레트 검증의 사각지대였다. 카테고리 팔레트 검증은 '표식끼리 구분되는가' 를 보지
> '글자로 읽히는가' 는 안 본다. 같은 색이라도 쓰임이 다르면 기준이 다르다.

### 3. 누르기 작은 것들

필터 칩 33px, '할 일 보기' 링크 22px, 스와이프 안내 닫기 29px 등.
보이는 크기를 다 키우면 화면이 헐거워지므로, **보이는 크기는 두고 누르는 영역만 44px 로 넓히는**
방법을 썼다 (`tap-44`). 칩만 32 → 36px 로 조금 키웠다.

> 검사도 같이 고쳤다. 처음엔 요소 상자 높이를 쟀는데, `::after` 로 넓힌 영역은
> 상자 크기에 안 잡혀서 고쳐도 계속 실패했다. 지금은 실제로 눌리는 높이를 잰다.
